const config = {
    port: 3000,
    database: {
        DATABASE: 'sys',
        USERNAME: 'root',
        PASSWORD: '520.mnbx',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config;